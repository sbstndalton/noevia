# Changelog drafts — `DaServer.md` rows, written before execution (rule: never after the fact)

These rows are appended to `DaServer.md`'s Changelog table as each gated step executes
(copy the row text verbatim; dates updated to the execution date).

## Row 0 — openwork/Lemonade provider config (Mac-side, spec §3)

| 2026-09-02 | OpenWork (Mac) configured against Lemonade: `~/.config/opencode/opencode.json` adds custom OpenAI-compatible provider at `http://10.69.0.130:13305/v1` (E4B default model, E2B small_model, GPT-OSS-20B listed). Config-only change on the Mac; Lemonade endpoint untouched and remains directly reachable. | Spec §3 first step — Mac-native visible-action track wired to the existing inference boundary; no build item, one-line changelog per spec. |

## Row 1 — doc-drift fix (Step A)

| 2026-09-02 | Diary Companion doc-drift fix: server `config/config.yaml` `llm.embed_model` corrected `nomic-embed-text-v1.5.Q4_K_M` → `nomic-embed-text-v1-GGUF`, aligning the file with the runtime value already set via `.env` (2026-09-02 changelog). Retrieval index still empty → zero re-index cost; no container restart needed (runtime env unchanged). | Spec §4 flagged item resolved before any retrieval work; file now matches the live embedding lock. |

## Row 2 — stack staged (Step B)

| 2026-09-02 | Cowork Workspace stack staged at `/mnt/docker/appdata/cowork/` (compose: anythingllm + litellm + diary-companion sidecar; joins `lemonade_default`, no new exposed inference ports; AnythingLLM on port 8020). Secrets: LITELLM_MASTER_KEY/SALT_KEY + AnythingLLM JWT_SECRET generated fresh; DIARY_AUTH_TOKEN and all WebDAV credentials COPIED from the standalone diary-companion `.env` (nothing regenerated). LiteLLM config: aliases diary→sidecar `/v1`, e4b/e2b→Lemonade; gpt-oss-20b alias staged but commented (model absent from Lemonade's current catalog — second doc-drift noted, Phase 1b decides); cloud escape-hatch blocks present but commented (Phase 2, enabled:false-equivalent). Spike scratch corpus `Diary-spike-test/` created in Nextcloud for gated verification. | Build-spec v0.4 §2/§4/§5 with researched correction: AnythingLLM Generic OpenAI base URL is instance-wide (upstream #4243/#4493/#5084), so per-workspace endpoint routing is provided by a LiteLLM gateway container (user decision 2026-09-02); v1 customization = vanilla image + theming (user decision). Standalone container left running untouched. |

## Row 3 — stack up (Step C)

| 2026-09-02 | Cowork Workspace stack started (`docker compose up -d`): cowork-anythingllm (8020), cowork-litellm (internal), cowork-diary-companion (internal sidecar, separate `data/` SQLite, corpus root overridden to `Diary-spike-test` for the spike). Port 8020 confirmed free before up. | Spec §10.2 spike — verify workspace model + alias routing against live Lemonade and the sidecar pipeline before any cutover decision. |

## Row 4 — spike verified + cleanup (Steps D/E)

| 2026-09-02 | Cowork spike verification complete: [RESULTS PLACEHOLDER — fill with actual observed outcomes: E4B via LiteLLM ok; diary round-trip to Diary-spike-test (format/markers/INDEX.md); skip-classifier skip; real Diary/ untouched]. Scratch corpus `Diary-spike-test/` removed (user sign-off); cowork stack stopped or left running: [STATE]. Real diary corpus unmodified throughout. | Spec §10.2 gate evidence for the §4 sidecar path; standalone container continues to serve production traffic until cutover sign-off. |

## Row 5 — sidecar 0.1.2 rebuild + stack drift-proofing (server step pending GO)

| 2026-09-03 | Cowork stack drift-proofed and sidecar pin corrected: `diary-companion` image pin moved `0.1.1` → `0.1.2` (picks up the Apache `-gzip` ETag fix already shipped in the standalone container) via tracked commit; upstream images `ghcr.io/mintplex-labs/anything-llm:latest` and `ghcr.io/berriai/litellm:main-stable` pinned by multi-arch index digest (captured 2026-09-03) so upstream pushes/retags cannot move the running stack; upgrade/release procedures documented in `UPGRADES.md`. Server side (PENDING GO): rebuild `diary-companion:0.1.2` from `/mnt/docker/appdata/diary-companion`, then gated `docker compose up -d` in `/mnt/docker/appdata/cowork/` — recreates only the sidecar; AnythingLLM/LiteLLM containers untouched (same digests as deployed). Rollback = revert the pin commit + `up -d`. | Keeps the sidecar stack's WebDAV behavior identical to the verified standalone (0.1.2 ETag fix) and converts upstream image drift from a silent `pull`-time risk into a deliberate changelogged upgrade path (MIGRATION.md ground rules). |

## Row 6 — Cowork UI built (frontend supersession; deploy = gated Step F)

| 2026-09-03 | Cowork frontend superseded per user decision: AnythingLLM rejected as the final surface (mockups unreachable inside it; its per-workspace endpoint feature already replaced by the LiteLLM gateway). Built `cowork/ui/` — React implementation of the four ui-mockups artboards (pinned Diary, Spaces, month/day/time transcript from the real corpus, right rail This month/Open questions/Timeline, model-pill headers, Settings roster from the gateway's live alias table, light Manrope + dark DirectionB theme toggle) plus a zero-dependency proxy server (SSE chat via LiteLLM aliases with the `diary` alias called exactly once per exchange; diary corpus reads via the sidecar's `/api/day`; per-space history JSON, atomic writes; secrets only via `ui.env`). Compose gains a `ui` service (host port 8021, internal network only + tunnel-eligible); AnythingLLM stays running untouched during burn-in. Verified Mac-side against the live stack over SSH tunnels: real-corpus rendering, E4B chat round-trip, history persistence, both themes. Server deploy is gated Step F in MIGRATION.md (stage + copy secrets, then GO-gated `docker compose build ui && up -d ui`). | User verdict on the AnythingLLM build; mockups are the spec. UI-agnostic plumbing (gateway aliases, sidecar, pinned compose) reused unchanged; no server state mutated before sign-off. |

## Row 7 — Cowork UI deployed (gated Step F, user GO)

| 2026-09-03 | Cowork UI deployed to DaServer: `cowork/` staged via rsync (excluding node_modules/dist/ui-data/secrets); `ui.env` created server-side by COPYING the existing LITELLM_MASTER_KEY + DIARY_AUTH_TOKEN (chmod 600, nothing regenerated); `docker compose build ui && up -d ui`. cowork-ui live on host port 8021, `/api/health` green (gateway + sidecar reachable in-stack). Note: compose also RECREATED cowork-litellm via the dependency edge (same digest-pinned image, same config — no functional change); cowork-anythingllm and cowork-diary-companion untouched (healthy, pre-existing uptime). | User sequencing decision: deploy verified v1 now, daily-drive it, then rework the Diary tab; AnythingLLM stays as burn-in fallback until the UI proves out. |

## Row 9 — UI v3: proxy-native routing, model manager, personas/memories, reasoning stream (deployed)

| 2026-09-03 | Cowork UI v3 deployed (user feedback round: model switching, downloads, thinking visibility, real settings, de-duplication). Proxy is now the router: ordinary spaces chat with Lemonade directly with per-space model + system prompt + persistent memories (spaces.json; UI-editable in the model popup's This-space tab and Settings' Spaces editor); the diary tab still routes through the sidecar exactly once per exchange. Model manager added end-to-end against Lemonade's management API: installed list (sizes, loaded state, hash-dupe IDs filtered), HF search via the public registry API (deployed Lemonade image predates /v1/registry/search), GGUF variant enumeration with sizes (`pull/variants?checkpoint=…`), pull, delete (confirm-gated), load/unload, download-job progress. Transcript streams reasoning_content as a live collapsible Thinking block and renders tool_call deltas as chips. LiteLLM retired from the chat path (container still running; removal gated). Compose: ui joins lemonade network, talks to Lemonade directly. Verified live: E4B answered a memory question correctly from injected persona+memory; search/variants returned real Qwen3 data; popup renders the real roster. | User's four-point feedback addressed; app backend is now fully ours (zero AnythingLLM/LiteLLM dependency on the hot path). |

## Row 10 — de-duplication plan (PENDING GO — nothing stopped yet)

| 2026-09-03 | PENDING — server de-duplication per docker inventory review (user: "reduce running applications"). Proposed order, each step gated on explicit GO: (1) stop `open-webui` (:3000, 657 MB — anythingllm already replaced its job; not deleted); (2) after Solair AI re-point confirmed, retire standalone `diary-companion` (:8010, currently UNHEALTHY — investigate before anything else; sidecar 0.1.2 healthy); (3) remove `cowork-litellm` from compose + stop (792 MB) once direct routing is daily-driven; (4) stop `cowork-anythingllm` (347 MB) after UI burn-in. Total recoverable ≈ 2.05 GB RAM and three fewer attack surfaces. Nothing is deleted; images/kept for rollback. |

## Row 11 — UI v4: Claude-style Projects + live Lemonade stats bar (deployed)

| 2026-09-03 | Cowork UI v4 deployed (user feedback round: stats readout + Claude-like projects). Sidebar is now project-based: Projects row with per-project tabs, Diary tab, Settings — the old flat "spaces" model is removed (spaces.json replaced by projects.json). Projects overview grid + create modal (name / goal / instructions / text-file attachments, injected into context); project detail = New-task composer + Recents (chat title auto-set from first message, delete per chat) + editable Instructions/Files/Memory rail; every project chat gets the project context injected server-side. NEW: live stats bar docked at the bottom of every view, polling Lemonade `/v1/stats` + `/v1/system-stats` every 2.5s via the proxy passthrough — tok/s, TTFT, tokens in/out, request count, CPU/GPU/VRAM. History persists per project/chat across reloads. Verified end-to-end Mac-side against the live stack: project create → chat with injected memory (correct answer from project instructions) → recents/persistence → stats updating live per request → project delete. Test data cleaned; prod workspace starts empty for the user's own first project. Deploy: `docker compose build ui && up -d ui` (only cowork-ui recreated). | User's stats + Claude-projects request; "delete the existing spaces — I'll test making one myself" honored (fresh workspace). Text attachments only — true doc-RAG stays a diary-sidecar extension (flagged, not scheduled). |

| 2026-09-03 | Diary reworked per user decision into a dedicated TAB (no "Pinned" workspace badge; no longer listed among Spaces): in-tab composer routes through the `diary` alias — every exchange flows the full sidecar pipeline, and the tab surfaces the pipeline's verdict per exchange (logged / skipped / error) via the sidecar response's `diary.decision`, refreshing the corpus view after send. Proxy gains a corpus-source adapter (`listMonths`/`readMonth` contract; v1 = `sidecar` source over the existing read API; `local` folder source designed with env switch `DIARY_SOURCE`, not yet implemented). Chat safety property preserved and mock-verified: the diary alias is called exactly once per exchange (non-streaming, no retry — a retry would double-log). New `/api/diary/source` endpoint; `/api/workspace` no longer lists a diary space. Redeployed to DaServer: `docker compose build ui && up -d ui` (only cowork-ui recreated; sidecar/AnythingLLM untouched). Verified server-side: adapter + spaces list live on :8021. | Diary becomes a first-class destination with the corpus harness seam the user asked for (Nextcloud now, local folders later); pipeline and corpus untouched — adapter reads only. |

## Row 12 — UI v5: provider layer, RAG, Auto routing, tools, skills (master steps 9–13, deployed)

| 2026-09-03 | Cowork UI v5 deployed (master sequence steps 9–13). Provider registry (`providers.json` + CRUD, keys masked server-side; `DEFAULT_MODEL` literal deleted — unset models resolve to whatever Lemonade has loaded). Project RAG: per-project sqlite-vec indexes (`ui-data/rag/<id>.db`, nomic-embed via Lemonade, content-hash dedupe, verbatim fallback on any RAG failure); runtime image moved Alpine → Debian (glibc) because sqlite-vec prebuilds fail on musl with the misleading `vec0.so.so` error. Auto routing: per-message Fast/Smart via deterministic heuristic + fail-open classifier, role mapping in `auto-roles.json` (UI-editable), manual projects untouched; measured caveat: Lemonade keeps one chat model resident and auto-loads on demand, so a role switch pays one load swap. Built-in tools: JSON-Schema function format, `get_current_time` + `read_project_file`, bounded 3-round tool loop with role:tool continuation, chips in transcript. SKILL.md files in projects advertised via L0 index, full body loaded on demand (progressive disclosure). Auto heuristic tuned on an 8-message battery to 8/8. Verified live on prod: RAG isolation (each project answers only from its own files), tool round-trip, Auto escalation, vec0 load check. Commits: 2a5da3f, a937f22, d73e653, b83f401, 75d6497, fc3605a. | Master sequence complete (12/12 + step-13 extras from the research spike); app is now self-hosted end to end: own provider layer, own retrieval, own routing, own tools. |

## Row 13 — research spike: Pi / Hermes Agent (report only)

| 2026-09-03 | Research spike (master step 11, feature doc Item 5) completed — no code adopted. Findings: Pi (MIT, TypeScript) validates Cowork's provider-registry design and contributes the tool-schema convention (JSON-Schema parameters + toolResult messages) now used by the tools layer; Hermes Agent (MIT, Python) contributes the SKILL.md convention (agentskills.io-compatible) now recognized in project files. Neither codebase is vendored; both agent runtimes are tightly coupled to their own processes. Report: `suggested changes/pi-hermes-research-2026-09-03.md`. | Input for future tools/skills decisions; conventions adopted at zero dependency cost. |
