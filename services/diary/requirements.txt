# Core
fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
pyyaml>=6.0
jinja2>=3.1
python-multipart>=0.0.9

# Storage / retrieval
sqlite-vec>=0.1.6
numpy>=1.26

# Dev / tests
pytest>=8.0
pytest-asyncio>=0.23
# Test-only: local S3-compatible server for backend contract tests (see
# tests/test_s3_backend.py). The runtime S3 client is stdlib + httpx only.
moto[server]>=5.0
