# Development continuation and QA — 2026-09-08

Based on Claude's clean `0f8a9c1` checkout. The older UI master prompt was
treated as design context; much of its implementation was already present.

## Fixes implemented

- Fresh startup creates the state directory before writing its encryption key.
- Development, build, and type-check scripts invoke Node directly. Vite uses
  the runner config loader, avoiding temporary writes inside the external
  dependency symlink.
- New project folders include the project ID, preventing same-name projects
  from sharing a folder. Existing folder paths are preserved.
- Uploading to an older project creates its missing storage folder on demand.
  Without remote storage, text/PDF uploads remain usable as local sources.
  S3 uses a prefix instead of attempting an unsupported directory operation.
- Image/document request caps account for base64 expansion while retaining
  decoded-byte limits. A 7 MB image no longer fails the advertised 8 MB limit.
- Failed source reads preserve the previous source text. Successful empty
  listings and explicit folder detachment still remove old sources.
- A refresh preserves uploads and folder changes made while it was awaiting
  storage. Opening a project refreshes its attached folders, throttled to once
  a minute when revisiting the same folder selection.
- Invalid project settings no longer partly mutate the live project.
- Debounced edits merge different changed fields instead of dropping all but
  the last patch. The edit dialog awaits saving and retains input on failure.
- Model/routing/toolbox save errors are displayed instead of being silently
  ignored or becoming unhandled promise rejections.
- Vision probes are scoped to endpoint, credentials, and model, with expiring
  results. Missing projectors and availability failures have distinct,
  actionable explanations shown in chat. Image-description cache keys include
  the user, provider endpoint, and full question.
- Provider authorization runs before image requests; vision requests refuse
  redirects, and description requests respond to chat cancellation.
- Repeated tool calls across inference rounds retain distinct chip identities.
  Pending approvals keep their full arguments and all three decision buttons.
- Mobile chat breadcrumbs truncate long names without hiding Settings or the
  inspector. Project headings reserve inspector space. Tool results use a
  compact layout; approval arguments remain fully visible and wrap.
- Mobile Settings uses a category selector so forms have the full screen width.
  Provider fields have accessible labels. The diary navigation-expand control
  is restored on phones.
- Project/model dialogs use native modality, Escape dismissal, and focus
  restoration. Keyboard activation of a card's child controls no longer opens
  the project. Filter/archive empty states are explicit.
- Creation uses the shared text-file validation rather than reading arbitrary
  selected binaries as text. Project initials use theme text contrast, and the
  initial browser theme color matches the dark canvas.

## Verification

- Web: **161 passed**, including 11 new regression tests.
- Diary: **145 passed, 3 skipped**; no diary implementation changes.
- `npm run typecheck`: passed.
- `npm run build`: passed.
- `git diff --check`: passed.
- Browser checks used an isolated authenticated instance at localhost:8022,
  disposable state, and a local inference simulator. No production diary
  messages, files, or tool writes were created.
- Checked phone (375px), tablet (768px), and desktop (1440px) layouts in light
  and dark themes across the work. Additional chat overflow checks at 320,
  640, 641, and 1024px found no clipped buttons or horizontal page overflow.
- Exercised onboarding, project creation, model selection, failed-chat retry,
  two successive clock-tool rounds, Markdown tables/code/parenthesized links,
  settings, diary home/calendar/error states, coding preview, and keyboard
  modal/card controls. Rechecked the mobile header, settings selector, and
  diary navigation fixes after rebuilding.

## Local setup findings

This Nextcloud checkout lost generated assets from `dist/assets` during QA.
The local ignored `apps/web/dist` now links to `/tmp/noevia-qa-dist`. Claude's
existing `node_modules` link to `~/.noevia-deps/node_modules` was preserved.
Both dependency installation and build output should stay outside folders that
the sync client evicts. The `/tmp` QA state and simulator are disposable.

The local Node runtime lacks the optional `sqlite-vec` dependency, so browser
smoke tests used the application's direct source injection fallback. A live
RAG/embedding smoke test remains necessary. Docker was unavailable on this Mac;
container builds and Compose validation were not run locally.

## Deployment and remaining live work

Read-only SSH verification confirmed DaServer still runs release `0f8a9c1`;
the diary container was healthy. The patch introduces no required environment
variables. The user subsequently approved publishing to GitHub and deploying
to production. Rollout results are recorded in the canonical DaServer changelog.

For deployment, follow the existing release runbook, record the
DaServer change, and verify actual Nextcloud uploads/deletions, MCP approval
decisions, source refresh, and vision from an authenticated browser. The
Lemonade Qwen projector installation/configuration described in Claude's
handoff remains to be diagnosed and repaired on the server. Existing projects
that already share a folder are not automatically split or moved.

The coding workspace and planned features remain previews. DOCX/OCR support,
chat branching, usage cost estimates, backup configuration, and optional
Tavily crawl policy remain separate backlog items; this pass does not claim
they are implemented or that exploratory QA can prove the absence of bugs.
